// import Toast from "react-native-simple-toast";

export const toastBottom = text => {
  // Toast.showWithGravity(text, Toast.SHORT, Toast.BOTTOM);
};

export const toastCenter = text => {
  // Toast.showWithGravity(text, Toast.SHORT, Toast.CENTER);
};

export const toastTop = text => {
  // Toast.showWithGravity(text, Toast.SHORT, Toast.TOP);
};

export const toastLong = text => {
  // Toast.show(text, Toast.LONG);
};

export const toastShort = text => {
  // Toast.show(text, Toast.SHORT);
};
